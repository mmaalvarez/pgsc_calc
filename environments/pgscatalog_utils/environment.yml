name: pgscatalog-utils
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - python>=3.10
  - pip
  - pip:
    - pgscatalog.core==1.0.0
    - pgscatalog.match==0.4.0
    - pgscatalog.calc==0.3.0
